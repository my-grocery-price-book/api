box: pharkle/ubuntu12.04-ruby2.2.0@1.0.1
build:
  steps:
    - bundle-install
    - script:
        name: rake
        code: bundle exec rake
deploy:
  steps:
    - add-ssh-key:
        keyname: DEPLOY_KEY
    - script:
        name: Capistrano
        code: bundle exec cap production deploy
