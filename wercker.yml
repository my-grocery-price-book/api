box: wercker/ruby
build:
  steps:
    - bundle-install
    - script:
        name: rake
        code: bundle exec rake
deploy:
  steps:
    - script:
        name: Ansible
        code: ansible-playbook -i hosts/production site.yml
    - script:
        name: Capistrano
        code: bundle exec cap production deploy
