box: wercker/box-rvm
build:
  steps:
    - bundle-install
    - script:
        name: rake
        code: bundle exec rake
deploy:
  steps:
    - add-ssh-key:
        keyname: DEPLOY_KEY
    - script:
        name: Capistrano
        code: bundle exec cap production deploy
